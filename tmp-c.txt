feat(R427): market/constraints brief fields gain char counters

Founder brief had counter pills on idea, audience and the context
textarea (R421), but the Market context single-line input and the
Constraints textarea had nothing, even though long responses in
either can bloat the prompt just as much.

Added id/aria-describedby plus right-aligned counters:
  * Market context (<input>): amber >120 chars, red >200; tooltip
    "Recommended under 120 chars".
  * Constraints (<textarea rows=4>): amber >320, red >500; tooltip
    "Recommended under 320 chars".
Both thresholds match the supporting-paragraph sizing used for
audience/context; market is shorter because it is a single-line
geography/segment field. No hard cap, pure soft nudge. tsc, eslint
and all 412 tests green; full Next build was not rerun (environment
timed out) but tsc covers the JSX typing.
