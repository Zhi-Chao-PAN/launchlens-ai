feat(R428+R430): shared page skip link + copilot batch sr fail count

R428: The read-only /share/[id] view renders a skip link at the very
top of <main> (sr-only, visible on focus) that jumps to
#validation-decisions. Keyboard and AT users landing on a long
shared workspace no longer have to Tab through every header action
(copy, download, link, Open demo, pills, metadata) before reaching
the validation decisions section.

R430: Decision Copilot batch-brief completion surfaced failures via
setError but the sr-only aria-live announcement only reported
success count ("X of Y generated"), so AT users heard no indication
of partial failure. A single srSummary string now mirrors the
visible message ("... N failed.") when failCount > 0 and feeds
both setError and setSrGenerationAnnouncement.
